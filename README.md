# Terraform-Infrastructure
This will create the simple infra structure: VPC (with 2 public and private subnet) private subnet is without NAT, RDS (MYSQL), 1ec2 instace, 2 security group, 1 IAM ssm role, elastic ip for the server.
